{# Nexus sidebar — bölüm başlığı (metismenu <ul> içinde <li> olarak kullanın) #}
{% macro section_label(title) %}
<li class="nx-menu-section-label" role="presentation"><span class="nx-menu-section-label__text">{{ title }}</span></li>
{% endmacro %}
